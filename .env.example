# Binance Spot DEMO API credentials
# binance.com → Demo Trading → API Management (Spot demo keys)
BINANCE_API_KEY=your_api_key_here
BINANCE_SECRET=your_api_secret_here

# Telegram alerts (optional — leave empty to disable)
# 1) Telegram'da @BotFather → /newbot → token al
# 2) Bota bir mesaj at, sonra chat id: https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Order size in USDT (quote spent on each spot market buy)
ORDER_USDT=100

# Max concurrent REST fetches during warmup
MAX_CONCURRENCY=20

# 1m pump hunter filters
# Entry uses PREVIOUS closed 1m quote vol only (no rel multiplier)
MIN_CANDLE_QUOTE_VOL=10000
VOL_LOOKBACK=20
VOL_MULT=3.0
MIN_CANDLE_PCT=2.5

# FBB band refresh interval (seconds)
FBB_REFRESH_SEC=90

# Exit hybrid: sell 50% at +3%, remainder on EMA9 1m (+ entry candle low stop)
# EMA_EXIT_TF=1m or 5m | USE_TRAIL=1 optional
PARTIAL_TP_PCT=3.0
PARTIAL_TP_FRAC=0.5
USE_TRAIL=0
EMA_EXIT_TF=1m
TRAIL_ACTIVATE_PCT=10.0
TRAIL_PCT=3.0
